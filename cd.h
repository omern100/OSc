#ifndef CD_H
#define CD_H

/**
 * @brief משנה את הנתיב של התהליך לפי הקלט של המשתמש
 * @details מטפל בנתיבים עם גרשיים או בלי, משתמש ב-chdir לשינוי התיקייה
 * @param args מערך של מחרוזות מהמשתמש
 * @return אין
 */
void cd(char **args);

#endif